import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pocketcodestudio.ghoxstmods',
  appName: 'pocket-code-studio',
  webDir: 'dist'
};

export default config;
